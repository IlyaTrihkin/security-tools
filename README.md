# 🛡️ security-tools

Универсальный набор скриптов для автоматизации задач информационной безопасности в Linux.

**Основной инструмент** — `audit.py` — универсальный скрипт аудита безопасности для Linux, который проверяет обновления, устанавливает их (опционально), анализирует логи SSH и генерирует подробный отчёт.

---

## 📚 Содержание

- [Основной скрипт аудита](https://github.com/IlyaTrihkin/security-tools#1--основной-скрипт-аудита-auditpy)
- [Установка и использование](https://github.com/IlyaTrihkin/security-tools#-установка-и-использование)
- [Параметры командной строки](https://github.com/IlyaTrihkin/security-tools#%EF%B8%8F-параметры-командной-строки)
- [Примеры использования](https://github.com/IlyaTrihkin/security-tools#-примеры-использования)
- [Запуск через Docker](https://github.com/IlyaTrihkin/security-tools#-запуск-через-docker)
- [Формат отчёта](#-формат-отчёта)
- [Структура проекта](https://github.com/IlyaTrihkin/security-tools#-структура-проекта)
- [Планы по развитию](https://github.com/IlyaTrihkin/security-tools#-планы-по-развитию)
- [Автор](https://github.com/IlyaTrihkin/security-tools#-автор)

---

## 1. 🎯 Основной скрипт аудита (`audit.py`)

`audit.py` — это единая точка входа для проведения аудита безопасности на Linux-системах.

**Что он делает:**
1. **Проверяет наличие доступных обновлений** (через `apt` или `yum`).
2. **При наличии обновлений** предлагает их установить (или делает это автоматически с флагом `--yes`).
3. **Анализирует логи SSH** (`/var/log/auth.log`) на предмет неудачных попыток входа (брутфорс).
4. **Выполняет проверку уязвимостей (CVE)** через публичное NVD API для всех или только критических пакетов (с фильтрацией по версиям).
5. **Собирает расширенную системную информацию** (имя хоста, тип ОС, версия ядра, серийный номер, MAC/IP, Machine ID).
6. **Генерирует подробный отчёт** в двух форматах:
   - **TXT** — структурированный.
   - **JSON** — для машинной обработки и интеграции.

---

## 📦 Установка и использование

### 📋 Требования

- Python 3.6+
- Менеджер пакетов `apt` или `yum`
- Права `sudo` (для установки обновлений и чтения системных файлов)

### Клонирование репозитория

```bash
git clone https://github.com/IlyaTrihkin/security-tools.git
cd security-tools
```

### Установка зависимостей

Рекомендуется использовать виртуальное окружение:
```bash
python3 -m venv venv
source venv/bin/activate
pip install requests packaging
```

### Запуск

```bash
python audit.py [опции]
```

### ⚙️ Параметры командной строки

| Параметр | Описание |
|----------|----------|
| `--yes, -y` | Автоматически устанавливать все доступные обновления без подтверждения |
| `--no-update` | Пропустить установку обновлений (только проверка) |
| `--cve` | Проверить все установленные пакеты на наличие известных уязвимостей (CVE) через NVD API |
| `--quick` | При проверке CVE проверять только критические пакеты (быстрее) |
| `-h, --help` | Показать справку |

### 💻 Примеры использования

#### Базовая проверка (с предложением обновить)

```bash
python3 audit.py
```

#### Автоматическая установка всех обновлений

```bash
python3 audit.py --yes
```

#### Только проверка, без установки

```bash
python3 audit.py --no-update
```

#### Проверка CVE для всех установленных пакетов

```bash
python audit.py --cve
```

#### Быстрая проверка CVE (только критические пакеты)

```bash
python audit.py --cve --quick
```

#### Комбинация: автоматическая установка + быстрая CVE

```bash
python audit.py --yes --cve --quick
```

#### Помощь

```bash
python3 audit.py --help
```

---

## 🐳 Запуск через Docker

Образ можно собрать и запустить в изолированной среде.

### Сборка образа

```bash
cd docker
docker build -t security-audit -f Dockerfile ..
```

### Запуск контейнера

```bash
# Справка
docker run --rm security-audit

# Проверка CVE (быстрый режим)
docker run --rm security-audit --cve --quick

# Автоматическая установка обновлений (требует --privileged или монтирование)
docker run --rm -it security-audit --yes
```

### Запуск через docker-compose

```bash
cd docker
docker compose up
```
Вы можете изменить команду в docker-compose.yml для нужного набора флагов.


Примечание: внутри контейнера доступ к системным файлам и права sudo ограничены. Для полноценной работы на реальном сервере рекомендуется использовать --privileged и монтировать /var/log и другие системные каталоги.

---

## 📋 Формат отчёта

### Текстовый отчёт (TXT)

```bash
============================================================
         ОТЧЁТ ПО ИНФОРМАЦИОННОЙ БЕЗОПАСНОСТИ
============================================================

Дата:          2026-06-21 14:55:27
Имя хоста:     HOME-PC
Тип ОС:        Ubuntu
Версия ОС:     24.04
Версия ядра:   6.18.33.1-microsoft-standard-WSL2
Серийный N:    N/A
MAC-адрес:     00:15:5d:4e:b0:6a
IP-адрес:      172.28.156.216
Machine ID:    54a7c25a4c3e4fbaaa2bb3943bffdea9

--- ОБНОВЛЕНИЯ ---
Доступных обновлений не найдено.

--- УЯЗВИМОСТИ (CVE) ---
Всего пакетов в проверке: 18
Успешно проверено: 18
Всего найдено уязвимостей (соответствующих версии): 141
Ни одна из найденных уязвимостей не может быть устранена обновлением.

--- Детали по пакетам ---
Пакет: bash (найдено: 10 уязвимостей)
  CVE-1999-1383 (CVSS: N/A), CVE-1999-0234 (CVSS: N/A), CVE-1999-1048 (CVSS: N/A), ...
  Рекомендация: обновлений для этого пакета не найдено. Рекомендуется проверить наличие патча в репозитории или применить временные меры.

...

--- НЕУДАЧНЫЕ ПОПЫТКИ ВХОДА ---
Неудачных попыток не обнаружено.

============================================================
Должность:               Специалист по информационной безопасности
Подпись:
ФИО:                     Илья Тришкин
Дата подписания:         21.06.2026
============================================================
```

### Текстовый отчёт (TXT)

Содержит те же данные в структурированном виде, может быть использован для автоматической обработки.

---

## 📁 Структура проекта

```bash
security-tools/
├── audit.py                      # ★ Главный скрипт аудита
├── modules/
│   ├── __init__.py
│   └── linux_audit.py            # Модуль с функциями (системная информация, обновления, CVE)
├── docker/
│   ├── Dockerfile                # Dockerfile для сборки образа
│   └── docker-compose.yml        # Оркестрация
├── reports/                      # Папка для сгенерированных отчётов (создаётся автоматически)
├── .gitignore
├── .dockerignore
└── README.md
```

---

### 🚀 Планы по развитию

- Добавление фильтрации CVE по минимальному CVSS-скору.

- Отправка отчётов по email (SMTP) и в Telegram.

- Интеграция с системами мониторинга (Zabbix, Prometheus).

- Упаковка в готовый Deb/RPM-пакет для простой установки.

---

## 👤 Автор

Илья Тришкин — специалист по информационной безопасности.

#### GitHub: 
https://github.com/IlyaTrihkin

#### TenChat: 
https://tenchat.ru/ilya_trishkin

#### Habr: 
https://habr.com/ru/users/ilya_trishkin
